services:
  bot:
    extends:
      file: compose.base.yml
      service: bot
    build:
      context: ..
      dockerfile: Dockerfile-cuda
    image: local/stt-tts-bot:cuda
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]